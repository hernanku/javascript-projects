pipeline {
    agent none 
    stages {
        stage('Code Build') {
            agent { docker 'node:13.12.0-alpine3.11' } 
            steps {
                echo 'Running npm build'
                sh 'npm install'
            }
        }
    }
}
