pipeline {
    agent {
        docker {
            label 'docker-node-agent'
        }
    } 
    stages {
        stage('Checkout code') {
            steps {
                checkout scm
            }
        }
        stage('Code Build') {
            steps {
                echo 'Running npm build'
                sh 'npm install'
            }
        }
    }
}
